'use client';

import { LeaguesTableContent } from './leagues-table-content';

/**
 * Leagues Table Component
 *
 * Main component that displays the leagues data table with all features:
 * - Data fetching and state management
 * - Pagination
 * - Column visibility
 */
export function LeaguesTable() {
  return <LeaguesTableContent />;
}
