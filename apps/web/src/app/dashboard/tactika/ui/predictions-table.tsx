'use client';

import { PredictionsTableContent } from './predictions-table-content';

/**
 * Tactika Predictions Table Component
 *
 * Main component that displays the predictions data table with all features:
 * - Data fetching and state management
 * - Pagination
 * - Sorting
 * - Filtering
 * - Column visibility
 */
export function PredictionsTable() {
  return <PredictionsTableContent />;
}
